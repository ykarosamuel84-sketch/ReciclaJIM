{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Abrir EcoJim no Chrome",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}